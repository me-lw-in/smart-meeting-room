spring:
  application:
    name: smart-meeting-room

  datasource:
    url: jdbc:mysql://localhost:3306/smart_meeting_room_dev
    username: root
    password: admin
server:
  port: 8080
  error:
    include-message: always